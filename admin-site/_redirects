# /api is reserved for Netlify Functions on this site (returns "Function not found").
# Browser calls /store-api/api/... (see config.js API_BASE); we proxy that to the boutique.
/store-api/*  https://boutiquemenes.netlify.app/:splat  200!
